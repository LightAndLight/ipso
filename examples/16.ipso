atob : < A : x | r > -> < B : x | r >
atob val =
  case val of
    A x -> B x
    r -> r

sayb : < B : x > -> IO ()
sayb _ = writeStdout stdout (toUtf8 "B")

main : IO ()
main = sayb (atob (A "hello"))