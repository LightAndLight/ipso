test : IO ()
test = 
  comp
    bind x <- readLineStdin stdin
    writeStdout stdout (toUtf8 x)

main : IO ()
main = 
  comp
    bind x <- readLineStdin stdin
    # comments are allowed between each line
  # here's another one
    writeStdout stdout (toUtf8 x)
         # and another
    test