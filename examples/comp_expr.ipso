test : IO ()
test = 
  comp
    bind x <- readln
    print x

main : IO ()
main = 
  comp
    bind x <- readln
    # comments are allowed between each line
  # here's another one
    print x
         # and another
    test