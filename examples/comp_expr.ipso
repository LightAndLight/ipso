test : IO ()
test = 
  comp
    bind x <- readLineStdin stdin
    print x

main : IO ()
main = 
  comp
    bind x <- readLineStdin stdin
    # comments are allowed between each line
  # here's another one
    print x
         # and another
    test