a : Int
a = let x = 1 in x

b : Int
b = 
  let x = 2 in 
  x

c : Int
c = 
  let 
    x = 3
  in 
  x

d : Int
d = 
  let 
    x = 4
  in 
    x

main : IO ()
main = 
  bindIO (writeStdout stdout (toUtf8 (showInt a))) (\_ ->
  bindIO (writeStdout stdout (toUtf8 (showInt b))) (\_ ->
  bindIO (writeStdout stdout (toUtf8 (showInt c))) (\_ ->
  writeStdout stdout (toUtf8 (showInt d))
  ))))