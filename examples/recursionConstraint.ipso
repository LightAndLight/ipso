class Show a where
  show : a -> String

instance Show () where
  show x = "()"

showMany : Show a => Int -> a -> String
showMany n x =
  if eqInt n 0
  then ""
  else "${show x}${showMany (subtract n 1) x}"

main : IO ()
main =
  writeStdout stdout (toUtf8 (showMany 5 ()))